# WP020 — #139 quota normalization contract

- Base/branch: ~~WP010 tip -> `codex/wibias-139-02-quota-contract`~~ **P-amendment (2026-07-17): commit directly onto dev (WP010 = 32a1b7fd).**
- P stale-check (2026-07-17, dev@32a1b7fd) — hunk-level dispositions:
  - TAKE `H229` (fetchCursorQuota ~170 lines, api2.cursor.sh period-usage → usage-summary → auth-usage fallback chain, reverseEngineered flag), `H230`-`H231` (oauth cursor dispatch + reverseEngineered on ProviderQuotaReport), `H232`-`H233` (inflight dedup + last-good row preservation + disabled-provider pruning in fetchProviderQuotaReports), `H227`-`H228` (anthropic quota moves from raw credential + REFRESH_SKEW_MS check to getValidAccessToken, matching the xai/antigravity pattern already on dev), `H226` only if REFRESH_SKEW_MS loses its last consumer after H227.
  - TAKE `H019`-`H020` (gui AccountQuota gains fiveHourPercent/fiveHourResetAt).
  - REJECT the source's isBuiltInChatGptForwardProvider string-match rewrite — it reverts the wp-020 hardening (isCanonicalOpenAiForwardProvider + OPENAI_MULTI_PROVIDER_ID). Keep the dev version verbatim.
  - ADAPT `H258`-`H269` (provider-quota tests): take the cursor fixtures, clearProviderQuotaCache setup/teardown, and last-good/inflight assertions, but KEEP `openai-multi` as the forward-provider test id (source renamed it to `openai`, which contradicts three-tier canon where the pool provider is openai-multi). clearProviderQuotaCache already exists on dev.
  - DEFER the two `tests/rate-limit-reset-credits.test.ts` hunks (CodexAuth.tsx → CodexAccountPool.tsx repoint) to WP060 — CodexAccountPool.tsx does not exist on dev yet.
- A-audit corrections (2026-07-17, Sol reviewer FAIL → amendments; all four accepted):
  - **Inflight epoch guard.** The source inflight dedup returns the old promise even for `forceRefresh=true` and survives `clearProviderQuotaCache()` — after an account switch (management-api.ts:1090 clears, Providers.tsx:100 forces refresh) the stale promise could repopulate the cache with the previous account's quota. Implementation: module-level `invalidationEpoch`; `clearProviderQuotaCache()` bumps it; **starting a `forceRefresh` probe ALSO bumps it** (an older in-flight non-forced probe loses commit authority — latest-started-wins, round-2 audit fix); a probe commits to cache only if its captured epoch is still current at completion; a forced call never joins an existing non-forced inflight. Tests: (a) clear-during-flight → result not cached, (b) non-forced probe A completing AFTER forced probe B must NOT overwrite B's cached result (assert final cache is B's), (c) forced call triggers its own upstream probe.
  - **Bounded last-good staleness.** Last-good preservation keeps rows alive forever while `generatedAt` renews and no GUI consumer reads row `updatedAt`. Implementation: cap preservation at `LAST_GOOD_MAX_AGE_MS = 30 * 60_000`; drop older rows; preserved rows keep their original `quota.updatedAt`/report `updatedAt` (never re-stamped). Tests: stale timestamp preservation, expiry past the cap, fresh-row replacement.
  - **Real fallback coverage.** Source fixtures for usage-summary/auth-usage are unreachable (period-usage succeeds first). Write three separate cursor tests driving each branch (period-usage OK; period-usage fails → usage-summary OK; both fail → auth-usage OK) plus disabled-provider pruning. Cursor test fixture uses `adapter: "cursor"` (realistic seed), not the source's `openai-chat`.
  - **UTC reset arithmetic.** The auth-usage next-reset uses local calendar components in the source; implement with `Date.UTC` month rollover and pin with a deterministic month-boundary test.
  - Anthropic replacement test must assert refresh was attempted and the usage endpoint NOT called on failure (source's tautological URL predicate is insufficient).
- MODIFY `src/providers/quota.ts`: retain provider-specific quota extraction, five-hour/weekly windows, reset timestamps, and checked-at/source metadata behind one normalized result.
- MODIFY `gui/src/codex-quota-utils.ts`: mirror only the normalized wire shape and plan-aware row inputs; no rendering.
- MODIFY `tests/provider-quota.test.ts`: add fixtures for primary/secondary windows, absent windows, stale metadata, and cursor probe behavior. (`tests/rate-limit-reset-credits.test.ts` deferred to WP060 — see P stale-check.)
- Before -> after: single/partial quota assumptions -> explicit multi-window contract with deterministic source/reset semantics.
- Conditional activation: fixtures must drive both five-hour and weekly windows and an absent-window fallback; assertions prove which branch fired.
- Verification: `bun test tests/provider-quota.test.ts tests/rate-limit-reset-credits.test.ts` (reset-credits must stay green untouched); `bun run typecheck`; `git diff --check`; `bun test --isolate tests` (authoritative full gate — plain run has 14 pre-existing failures); diff <=500 **(C-amendment 2026-07-17: the A-audit added five mandatory regression tests — two epoch races, interleaved-config inflight, forced-probe independence, staleness trio — pushing the total to 511 lines, of which 268 are tests. The 500-line reviewability gate is retained for implementation+contract surface (243 lines here, well under); audit-mandated test lines above the cap are accepted rather than splitting a single atomic contract into two children. Recorded in goalplan ledger.)**
- Rollback: remove only quota contract additions; WP010 catalog remains valid.
