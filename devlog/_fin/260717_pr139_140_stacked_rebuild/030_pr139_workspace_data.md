# WP030 — #139 pure workspace data helpers

- Base/branch: ~~WP020 tip -> `codex/wibias-139-03-workspace-data`~~ **P-amendment (2026-07-17): commit directly onto dev (WP020 = 982fbf4).**
- P stale-check (2026-07-17, dev@982fbf4):
  - Neither `gui/src/provider-workspace-data.ts` nor `tests/provider-workspace-data.test.ts` exists on dev — the source monolith (400 lines, hunk H208) is NEW code to decompose, not a diff to apply. Test file (H272, 483 lines) is rewritten fresh.
  - gui does NOT import from `src/` (separate compile units; codex-quota-utils.ts pattern = mirror wire shapes locally). So `isAccountProvider` gets a LOCAL canonical-forward predicate in `gui/src/provider-workspace/catalog.ts` mirroring `isCanonicalOpenAiForwardProvider` (adapter `openai-responses` + authMode `forward` + normalized base `https://chatgpt.com/backend-api/codex`), keyed to names `openai` / `openai-multi` (+ legacy `chatgpt`).
  - Source's `hideRedundantChatGptForwardProviders`/`isChatGptForwardProvider`/`pickChatGptForwardProvider` predate three-tier: replace with the canonical predicate; hiding logic only hides legacy `chatgpt` when canonical `openai` covers the same passthrough (per interview: replaced by wp-020 semantics).
  - Source's legacy deprecated aliases (ProviderRecord, buildProviderWorkspaceSections) have zero consumers on dev — drop them from the child.
  - provider-icons hunks H205-H207: TAKE brand-colors/display-names/`providerBrandColor`/`formatProviderDisplayName`/`isCatalogProviderId` (pure data lookups, no rendering) BUT (a) keep the `openai-multi` alias the source removed, (b) align display names with three-tier registry labels: `openai` = "Codex Direct", `openai-multi` = "Codex Multi-account", `openai-apikey` = "OpenAI API" (source's `openai: "ChatGPT"` contradicts dev registry.ts:248-268), keep `chatgpt` = "ChatGPT" for the legacy id.
  - Interview tier decision applied at decomposition time: `isFreeProvider` = freeTier || keyOptional || authMode local || loopback (NO forward); `isAccountProvider(name, p)` = canonical forward OpenAI names; `providerTier(name, p)` returns "accounts" | "free" | "paid" (accounts wins over free); `buildProviderWorkspace` ready items carry `tier`; sort modes: az | za | free-paid | paid-free | accounts-first.
- Interview decision: 3-way classification (Free/Paid/Accounts). Remove `authMode === "forward"` from `isFreeProvider`. Add `isAccountProvider(name, p)` that returns true for canonical forward OpenAI providers (openai, openai-multi). `buildProviderWorkspace` returns `{ ready, needsSetup, disabled }` where ready items carry a `tier: "free" | "paid" | "accounts"` tag. Sort modes add `accounts-first` option.
- A-audit notes (2026-07-17, Sol reviewer PASS with 3 implementation notes):
  1. Export `pickCanonicalForwardProvider(providers)` from catalog.ts as the named successor of the source's `pickChatGptForwardProvider` (consumer: focusChatGptAuth deep link, WP080/091). Preference order: `openai-multi` first (account-pool auth is what the deep link serves), fall back to `openai`, then any other canonical match.
  2. Mirror dev `normalizedBaseUrl` (openai-tiers.ts:19-28) EXACTLY: strict casing (no lowercase), reject userinfo/query/hash (return undefined), no raw-string fallback when URL parsing throws.
  3. `authMode "local"` is defensive-only (dev wire never emits it — provider-payload.ts:44 drops it); loopback baseUrl does the real local classification. Tests must pair any "local" fixture with a loopback URL, never rely on authMode alone.
  4. provider-icons: taking the source hunk brings `momo` alias/color/name — acceptable. Keep `openai-multi` alias AND add its display name so `isCatalogProviderId("openai-multi")` is true.
  5. i18n `time.*` keys used by `relativeTimeLabelsFromT` are added by the consuming WP (080/090), not here — the helper only types `t`.
- NEW `gui/src/provider-workspace/catalog.ts`: provider readiness, free/paid classification, local/self-hosted classification, sorting, redundant ChatGPT-forward hiding.
- NEW `gui/src/provider-workspace/usage.ts`: model-count, usage-total, relative-time and attention-list pure helpers.
- MODIFY `gui/src/provider-icons.ts`: retain aliases/src lookup only; no component rendering.
- REWRITE `tests/provider-workspace-data.test.ts` into focused catalog/usage suites; do not mirror implementation branches tautologically.
- DELETE/avoid `gui/src/provider-workspace-data.ts` monolith in the child result.
- Before -> after: no workspace model -> small React-free owners with stable inputs/outputs.
- A-audit corrections (2026-07-17, claude-fable-5 reviewer via local proxy — subagent credits exhausted; 3-round audit, 1 blocker + minor notes folded):
  - **gui typecheck gate added (blocker).** Root `bun run typecheck` covers src/ only; every child touching gui/ must also run `cd gui && bun x tsc -b --noEmit` (or `bun run build`). Applies to this and later GUI children.
  - 500-line gate ruling: NEW-file children are exempt (gate bounds rewrite risk in existing files); this child's ~600 new lines across catalog/usage/tests are one child.
  - Hiding keys on the LITERAL id `chatgpt` only — `openai-multi` matches the canonical shape and must NEVER be hidden; pin with a test. Input map not mutated.
  - Keep forward ⇒ ready in isConfigurationReady (otherwise Codex Direct/Multi bin as needsSetup and surface as "Missing credentials").
  - Display names: also add `github`→GitHub, `github-copilot`→GitHub Copilot, `gitlab-duo`→GitLab Duo, `cloudflare-ai-gateway`→Cloudflare AI Gateway, `vercel-ai-gateway`→Vercel AI Gateway, `opencode-zen`→OpenCode Zen (title-case fallback would mis-brand them). Drop the source's `momo` additions (not a dev id) — conscious decision.
  - Split coupling: catalog keeps hasLoopbackBaseUrl/isConfigurationReady/isFreeProvider/sortWorkspaceItems/binProviderStatus together; usage imports catalog one-way (WorkspaceSections for buildAttentionItems); EN_RELATIVE stays private beside formatRelativeTime + relativeTimeLabelsFromT.
  - Test pins per reviewer list: tier boundary matrix (canonical + trailing slash, accounts>free precedence, myproxy paid, non-canonical base paid, nvidia needsSetup/ready duality, loopback trio incl [::1], malformed URL no-throw), hiding matrix (repointed chatgpt kept, absent openai kept, openai-multi never hidden), sort stability (accounts-first rank order, case-equal stability in az AND za, paid-free ascending tiebreak, no input mutation, mostUsed drops 0/negative/non-number), formatRequestCount en/de characterization (1.0k no-trim, 1000.0k boundary, de "1,20 Mrd." asymmetry, locale normalization de-DE/DE/de_AT/fr), formatRelativeTime edges (59999ms/60000ms/3599999/3600000/86399999/86400000, future clamp, 3 overload shapes), attention precedence (override wins, disabled-without-reason excluded, ready never included, needsSetup-before-disabled order), parse* malformed inputs + cross-key drift guard (selected vs available twins), countAvailableModels composition.
- Verification: `bun test tests/provider-workspace-data.test.ts`; `bun run --cwd gui lint`; `bun run typecheck`; `cd gui && bun x tsc -b --noEmit`; `git diff --check`; NEW-file exemption applies to the 500-line gate.
- Rollback: pure helper modules can revert without touching APIs/UI.
