# WP170 — #140 bounded image normalization

- Base/branch: ~~WP160 tip -> `codex/wibias-140-12-bounded-image-normalization`~~ **P-amendment (2026-07-17): commit directly onto dev@dc728af4 (zero-overlap reorder; src/adapters/anthropic-image-normalize.ts untouched by the concurrent session).**
- P stale-check (2026-07-17): the source hunk is a single mechanical rewrite of the sequential first-pass loop in `normalizeImageTargets` into an unbounded `Promise.all(targets.map(...))` — up to `processLimit` (anthropic passes 100) concurrent `processAt` decode/encode pipelines with no limiter. REJECT the unbounded form; ADAPT into a small explicit concurrency limiter (`IMAGE_NORMALIZE_CONCURRENCY = 4`, module const with rationale comment): a shared work-queue over indices where `min(limit, n)` workers pull the next index — preserves per-index entry writes (aggregate demotion loop after the pass is untouched and stays sequential). Failure semantics: a rejected `processAt` already resolves to `{kind:"failed"}` → drop; a THROWN error from encode/validate must reject the whole normalize call after all in-flight workers settle (no unhandled rejections, no leaked work). Order: entries[] is index-addressed so output order is deterministic regardless of completion order.
- Test plan (tests/anthropic-image-normalize.test.ts exists, 239 lines — APPEND a bounded-parallelism describe): >=8 base64 inputs with an instrumented `encode` recording active/peak counters via gated promises; assert `peak <= 4` AND `peak > 1` (actually parallel); rejection propagation (one thrown encode rejects the call, all settled, no unhandled rejection); output-order determinism (entries respect input order under scrambled completion order); URL-source and over-limit skip paths still bypass work.
- MODIFY `src/adapters/anthropic-image-normalize.ts`: retain parallel normalization only through an explicit small concurrency limiter; never unbounded `Promise.all` across up to 100 large images.
- Do not import unrelated `src/adapters/anthropic.ts` Doctor churn unless a direct caller contract requires it.
- MODIFY `tests/anthropic-image-normalize.test.ts` with at least eight inputs, instrumented active/peak counters, one failure, and output-order assertions.
- Before -> after: sequential baseline / source's unbounded fan-out -> bounded parallelism with deterministic order and cleanup.
- Conditional activation: test proves `peak <= limit`, `peak > 1`, rejection propagation, and no work leak after failure.
- Verification: `bun test tests/anthropic-image-normalize.test.ts tests/anthropic-image-guard.test.ts`; `bun run typecheck`; `bun run privacy:scan`; `git diff --check`; diff <=500.
- Attribution: maintainer repair + Wibias co-author because the reviewed source behavior is unsafe as written.
- Rollback: adapter-local child reverts without touching GUI/update/Cursor paths.
