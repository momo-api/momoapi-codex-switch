# WP040 — #139 safe provider management API

- Base/branch: ~~WP030 tip -> `codex/wibias-139-04-provider-management-api`~~ **P-amendment (2026-07-17, revised after the single-provider-option session landed 4da9c167 + 14e57661): commit directly onto dev@14e57661.**
- P stale-check v2 (2026-07-17, per-hunk against the v2 single-openai tree):
  - Tree changed under this WP: openai-multi is now legacy (absorbed into single `openai` + `codexAccountMode` pool/direct); PATCH /api/providers was REWRITTEN by that session into a strict exactly-one-of {disabled, codexAccountMode} handler with mode-change side effects (quota cache clear, thread map clear, pool prime). The source's broad PATCH (adapter/baseUrl/defaultModel/apiKey/authMode/note field-mask editor) must NOT replace it — it would drop the mode side-effect path and reintroduce a secrets-adjacent editor without pool integration.
  - Dispositions:
    - **PATCH field-mask (H247-portion): ADAPT, additive.** Extend the v2 PATCH to accept the editor fields (adapter/baseUrl/defaultModel/authMode/note) ALONGSIDE the existing exactly-one contract by relaxing to: body must be a plain object with >=1 recognized field; `codexAccountMode` remains mutually exclusive with everything else (its side-effect path stays intact and openai-only); editor fields validate via providerManagementConfigError + providerDestinationResolvedError before save; registry static headers stripped; model cache cleared + catalog refreshed. `apiKey` in PATCH: REJECT — key writes must go through the existing api-keys owner (REUSE clause below); the field-mask returns an error directing to the key endpoint instead of silently single-field-writing a key outside the pool.
    - **`authMode: "local"` union (deferred here from WP010): LAND now with the guard.** Add "local" to the OcxProviderConfig authMode union; the guard lives INSIDE `providerManagementConfigError` (shared by POST and merged-PATCH validation — A-audit rule, no per-handler duplication): reject `authMode: "local"` when the registry entry's authKind !== "local". Hand-edited config.json is an operator trust boundary — load-time guarding is out of scope for this WP (recorded). PATCH validation runs on the MERGED provider object, never the raw patch body (the canonical-seed guard then covers openai edits automatically). Regression test: nvidia PATCH to local → 400.
    - **/api/providers/test connectivity probe (H249-portion): TAKE with repairs (mechanism pinned per A-audit).** The source reports ok:true for any nonzero catalog model count, and `fetchAllModels` carries NO live-vs-seed provenance (its fallback chain hides fetch failures). Pinned mechanism: the endpoint performs the per-provider live probe DIRECTLY — `buildModelsRequest(prov, apiKey, name)` from src/oauth (same builder as catalog.ts:1187) + fetch with an 8s abort — and reports ok:true only on an HTTP-ok, schema-valid live response (count and latency from that fetch). Fetch failure/timeout/malformed → ok:false with the upstream error; `liveModels === false` or oauth-not-logged-in → ok:false "static catalog only — upstream not verified"; forward providers keep the passthrough message; disabled → ok:false. The aggregate fetchAllModels path is NOT used. NEW tests/provider-connection-test.test.ts drives: unreachable 127.0.0.1:1, fake-key non-ok, liveModels:false static-only, disabled, forward.
    - **oauth login/cancel endpoint (H252-portion): TAKE with `isPublicOAuthProvider`** (pinned at A: cancel is symmetric with /api/oauth/login which uses the public predicate — you can only cancel a flow the public surface could start; do NOT weaken /api/oauth/status validation as the source did).
    - **isPublicOAuthProvider → isOAuthProvider swaps + status-validation removal (source): REJECT** — dev's hardened public-surface predicate stays.
    - **PATCH body-size guard (H247 first hunk): REJECT (A-audit inversion catch)** — dev ALREADY includes PATCH in the 2MiB cap; the source hunk would REMOVE it (POST||PUT||PATCH → POST||PUT). No-op.
    - **server/index.ts hunks (H246): REJECT** — the source predates v2 startup migration (runOpenAiTierStartupMigration) and would remove it; chatgpt seeding logic conflicts with v2 legacy handling.
    - **auth-cors hunks (H243-H245): REJECT the deletions** (ForwardAdmissionCredentialError, requireResponsesApiAuth, FORBIDDEN_PROVIDER_RUNTIME_FIELDS, canonical-seed guard are all live v2 hardening); the source built them down because it predates them. `safeConfigDTO` freeTier exposure (WP010 follow-up note): ADD freeTier to the DTO whitelist so the GUI can render the Free badge from live config.
    - **tests/server-auth.test.ts (H275): ADAPT additively** — the source deletes ~850 lines of dev hardening tests; instead append: PATCH field-mask happy/validation paths, PATCH apiKey rejection, PATCH exactly-one-mode preservation, local-guard 400, freeTier in safeConfigDTO.
  - REUSE `src/providers/api-keys.ts` unchanged: the existing PUT/POST key endpoints already route through the pool owner; this WP adds no new key write path.
  - **Split ruling (A-audit, 500-line budget):** estimate 485-600. If B approaches the cap, split /api/providers/test + tests/provider-connection-test.test.ts into sub-child WP041 (self-contained); the PATCH/local/freeTier/cancel core commits first.
- MODIFY `src/server/management-api.ts`: add only provider workspace endpoints/mutations needed by later UI.
- Interview decision: scope decided per-phase at P. Look at the actual diff hunks, decide what to keep, and record the rationale in this document before B.
- REUSE `src/providers/api-keys.ts`: API-key writes must update active key and pool through the existing owner; do not duplicate pool logic in the HTTP handler.
- MODIFY `src/server/auth-cors.ts`, `src/server/index.ts` only for exact endpoint exposure/registration.
- MODIFY `tests/provider-api-keys.test.ts`: key update keeps active key, pool, and failover order aligned.
- NEW `tests/provider-connection-test.test.ts`: unreachable `127.0.0.1:1` + static catalog and fake-key cases cannot report a live connection.
- Before -> after: catalog-presence pseudo-test and single-field key write -> real upstream evidence and atomic key-pool mutation.
- Conditional activation: tests force unreachable upstream, stale/static model fallback, and multi-key update paths and observe the expected error/pool state.
- Verification: `bun test tests/provider-api-keys.test.ts tests/provider-connection-test.test.ts tests/server-auth.test.ts tests/openai-provider-tiers.test.ts`; `bun run typecheck`; `bun run privacy:scan`; `git diff --check`; diff <=500.
- Rollback: endpoints revert without removing WP010-030 contracts.
