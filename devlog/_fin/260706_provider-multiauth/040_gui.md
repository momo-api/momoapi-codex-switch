# 040 — GUI: card divider removal + account dropdown

## MODIFY gui/src/styles.css
- `.provider-quota { border-top: 1px solid var(--border-soft); padding-left: 58px; }`
  → remove `border-top` (user: "중간선 없애고"). Keep padding.
- NEW `.prov-accounts` styles: thin full-width row under the card body with a
  chevron toggle (`.prov-accounts-toggle`, height ~26px, muted, centered), and
  an expandable list `.prov-accounts-list` (rows: dot + email + active badge /
  "switch" affordance + per-account logout icon + trailing "add account" row).
  Rows are buttons; active row uses accent dot + badge-primary "Active".

## MODIFY gui/src/pages/Providers.tsx
- State: `accountSets: Record<provider, { activeAccountId, accounts }>`,
  `openAccounts: Record<provider, boolean>`.
- Fetch accounts for `authMode === "oauth"` providers (skip forward/passthrough —
  Codex pool is separate per user instruction) alongside fetchOauth.
- Card render (oauth cards only): after quota block (or after main when no
  quota), render the toggle: `계정 N` + chevron (rotates when open). Dropdown
  lists accounts; clicking a non-active account → PUT active → refetch accounts
  + quotas + notify. `+ Add account` row → POST /api/oauth/login {provider,
  addAccount:true} reusing existing loginOAuth polling (extract to accept
  addAccount flag). Per-account remove (small trash icon) → DELETE.
- Only oauth cards with ≥1 account show the toggle. Cards with 1 account still
  show it (entry point for adding the second account).
- i18n keys (en/ko/zh): prov.accounts ("Accounts {n}"), prov.accountActive,
  prov.accountSwitch, prov.accountAdd, prov.accountRemoved, prov.accountSwitched.

## Verify
- gui tsc + vite build; browser screenshot: divider gone, dropdown opens, switch
  updates Active badge; mobile 390px no overflow.
