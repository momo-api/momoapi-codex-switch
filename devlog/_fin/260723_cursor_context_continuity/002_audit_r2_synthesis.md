# Audit round 2 synthesis (same reviewer, VERDICT: FAIL, 6 blockers)

| # | Sev | Decision | Resolution |
|---|-----|----------|------------|
| 1 | High | ACCEPT | Byte accounting must survive load/reset/replace: one measured-entry constructor (`measuredEntry(items, providers)`) + one deletion helper (`deleteEntry(id)`) own ALL map mutations; `ensureLoaded` recomputes sizes for every snapshot version (never trusts persisted sizeBytes); `clearResponseStateMemoryForTests` zeroes the counter; `set` on an existing id subtracts the old entry first. Tests: restart-recompute, overwrite, TTL/count/byte eviction. 010 amended. |
| 2 | Med | ACCEPT | Seams named now: cap hook = exported `setResponseStateByteCapForTests(bytes|null)` (test-only setter, mirrors existing clear-for-tests convention at state.ts:221); server test = extend the existing `resolveAdapter` mock in tests/server-combo-failover-e2e.test.ts:35 with a branch returning `createCursorAdapter(provider, { createTransport: fakeFactory })` — adapter.name stays "cursor" because createCursorAdapter sets it. 010 amended. |
| 3 | High | ACCEPT | "resource_exhausted: too many requests" must stay 429. Predicate redesigned: quota/rate rejection FIRST ("too many requests", "quota", "rate limit", "rate-limit", "throttl") → false; then phrase-specific size patterns only: "tool catalog too large", "tool registration too large", "too many tools", "message too large", "payload too large", "request too large", /request exceeds .*(size|limit)/, "maximum allowed size". No free cue pairing. End-to-end safeCursorErrorMessage negatives added. 020 amended. |
| 4 | High | ACCEPT | Real-h2 integration tests can't count private fail/finish or distinguish close() vs destroy(). Unit seam: extract `createTerminalSettler({fail, finish, clearTimer})` into a small exported helper (same file), unit-test double-fire races directly; 3b destroy fallback tested via an injectable `destroyGraceMs` + a fake stream/session pair passed to an extracted `armTimeoutCleanup(stream, session, graceMs)` helper. Real-h2 tests stay as integration smoke only (no callback counting claims). 030 amended. |
| 5 | Med | ACCEPT | Cursor branch bypasses the existing failure cooldown: on discovery failure call `markModelsFetchFailure` (machinery at model-cache.ts:22) so repeated polls during outage don't each pay ~11.5s. Small addition at catalog.ts:1536 branch, with a test. 030 amended. |
| 6 | Med | ACCEPT | Same-ID acceptance scoped to native/non-rotating requests; external-model tool-result rotation (request-builder.ts:180) is intentional and stays. E2E chain test uses a native cursor model with a plain text continuation; separate case asserts external-model rotation + rekey + persistence of the NEW id. 010 amended. |

No rebuttals. Cross-blocker: #4's extracted helpers keep 3-pre semantics identical
(settleFail/settleFinish become the returned functions of createTerminalSettler).
