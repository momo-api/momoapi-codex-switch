# 010 — v2.7.27 릴리스 (outcome: DONE)

## 순서 (release.ts 정규 플로우)

1. dev → preview FF 머지 + push (pre-push 훅: `bun test --isolate` 3131 pass / 0 fail,
   privacy scan pass — 로컬 전체 스위트 플레이크는 `--isolate`에서 재현 안 됨을 확인).
2. `bun scripts/release.ts 2.7.27-preview.20260720 --tag preview --publish` →
   preview Release 워크플로우 publish 잡 44s 성공 (registry smoke + GitHub release 포함).
3. preview → main FF 머지 + push.
4. `bun scripts/release.ts 2.7.27 --publish` → stable publish 잡 39s 성공.
5. main → dev FF 역머지 + push (dev = 8715d180, v2.7.27).

## 검증

- npm dist-tags: `latest: 2.7.27`, `preview: 2.7.27-preview.20260720`.
- CI on 8715d180: Release / Cross-platform CI / Service lifecycle / React Doctor
  전부 success.

## 포함된 변경 (2.7.26 → 2.7.27)

- Windows 서비스 레인: VBS hidden 런처, WinSW `--native` opt-in, backend 보존
  업데이트, fail-closed WinSW 상태 probe (#165, #166).
- Claude authMode 영속화 + settings.json env 납치 방어.
- `ocx update --help` 부작용 제거 (#168) + GUI 업데이트 워커 child stdio 파이프
  (port #167).
- GUI: 모델 탭 활성 모델 상단 정렬.
