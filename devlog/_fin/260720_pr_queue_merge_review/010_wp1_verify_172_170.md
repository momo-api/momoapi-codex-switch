# 010 — WP1: #172 (react-doctor CI 수정) + #170 (stall timeout 300s) 검증

## 목표 verdict

둘 다 1차 판정 `approve-after-rebase`(#172는 즉시 머지 후보). 이 사이클은
그 판정을 로컬 증거로 확정하거나 뒤집는다.

## #172 검증 항목 (5 files)

1. `gh pr diff 172` 재확인 — 변경이 아래 5개뿐인지:
   `.github/workflows/react-doctor.yml`, `CONTRIBUTING.md`, `package.json`,
   `scripts/setup-hooks.ts`, `tests/ci-workflows.test.ts`.
2. 로컬 재현 + 테스트:
   ```
   # #172 — main에 있는 테스트만 사용
   git worktree add /tmp/ocx-pr172 FETCH_HEAD --detach
   # (FETCH_HEAD는 직전 fetch 결과; 또는 명시적 SHA 사용)
   cd /tmp/ocx-pr172
   bun install && bun test tests/ci-workflows.test.ts
   bun run lint:gui   # 신규 스크립트 존재 확인
   cd /Users/jun/developer/new/700_projects/opencodex
   git worktree remove --force /tmp/ocx-pr172
   ```
3. 워크플로 YAML 대조: `pull-requests: read` 존재, 액션 핀 =
   `938008119a288f2fb47c66a69cd9279a21f31784`, `blocking: none`, `: write`
   부재 — 테스트가 이미 단언하지만 눈으로 교차 확인.
4. `package.json` prepush 체인 순서: typecheck → lint:gui → test →
   privacy:scan → doctor:gui:if-changed.
5. (정보) fork PR이라도 workflow 파일 변경은 머지 전까지 base 것이 실행되므로
   #172 머지 후 다른 PR 리베이스 시 react-doctor가 초록으로 돌아와야 함.

## #170 검증 항목 (14 files)

   > **NOTE (A-gate fold #1):** `tests/stall-timeout.test.ts`는 #170 신규 파일이라
   > main에 없음. 반드시 #170 PR head를 scratch worktree에 checkout 후 실행.

1. 로컬 테스트:
   ```
   git fetch origin pull/170/head
   git worktree add /tmp/ocx-pr170 FETCH_HEAD --detach
   cd /tmp/ocx-pr170
   bun install
   bun test tests/stall-timeout.test.ts tests/web-search-timeout-plan.test.ts \
     tests/web-search.test.ts tests/bridge-lifecycle.test.ts tests/bridge.test.ts
   cd /Users/jun/developer/new/700_projects/opencodex
   git worktree remove --force /tmp/ocx-pr170
   ```
2. `src/stall-timeout.ts` 재독: `resolveStallTimeoutSec`의 ceil/min-1/non-finite
   분기가 테스트 케이스와 1:1 대응하는지.
3. `src/web-search/index.ts` 마진 산수: `webSearchStallTimeoutSec`가
   `DEFAULT_STALL_TIMEOUT_SEC`(300) import로 바뀌고 로컬 상수 중복 제거됐는지,
   `STALL_MARGIN_SEC=30` 유지인지.
4. 문서 정합: docs-site en/ko/zh + structure/04 의 90→300 전부 치환됐는지
   `rg -n 'stallTimeoutSec|stall deadline|90' docs-site structure` 로 잔여 90
   탐지(정당한 90 예: 사용자 설정 예시).
5. 행동 차이 기록: 구버전에서 NaN config가 워치독을 죽이던 것, 소규모 유닛
   버짓 조합의 web-search stall 하한 120→330s 상승 — verdict 문서에 명시.

## C-ACTIVATION-GROUNDING-01 시나리오

- #170: `resolveStallTimeoutSec(NaN)` → 300, `(0)` → 1 반환을 테스트가 직접
  단언(이미 존재) — 실행 증거는 위 bun test 출력.
- #172: 워크플로 변경의 activation은 CI에서만 발화. 로컬에서는
  `tests/ci-workflows.test.ts`의 YAML 단언 + #172 자체 런의 react-doctor 그린
  (run 29723211000)을 증거로 채택. 로컬 재현 불가를 verdict 문서에 명시.

## 판정 규칙

- 전부 통과 → #172 `approve`(즉시 머지 권고 1순위), #170 `approve-after-rebase`.
- 하나라도 로컬 실패 → 해당 PR `needs-work` + 실패 출력 첨부.

---

## Verdict (WP1 결과)

### #172 — `approve` (즉시 머지 1순위)

근거:
- `bun test tests/ci-workflows.test.ts`: 10/10 pass, 100 expects, 41ms.
- YAML 대조: `pull-requests: read` L28, 액션 핀 `938008…` L48, `blocking: none` L55,
  `: write` 0건.
- prepush 체인: `typecheck → lint:gui → test → privacy:scan → doctor:gui:if-changed` 정확.
- #172 자체 CI 런(29723211000): react-doctor 포함 전 매트릭스 그린.
- fork PR이므로 워크플로 파일 변경은 머지 후 base에 적용됨 — 다른 PR 리베이스 시
  react-doctor 그린 복원 예상.

### #170 — `approve-after-rebase` (#172 머지 후)

근거:
- `bun test` 5파일 68/68 pass, 200 expects, 5.72s (bun install 후 zod/v4 해석 정상).
  초회 2 fail은 worktree bun install 미실행 상태의 zod 미해석 — PR 코드 무관.
- `src/stall-timeout.ts`: `DEFAULT_STALL_TIMEOUT_SEC = 300`, `resolveStallTimeoutSec`
  ceil/min-1/non-finite 분기 확인.
- `src/web-search/index.ts`: `DEFAULT_STALL_TIMEOUT_SEC` import로 로컬 상수 중복 제거.
- docs-site en/ko/zh + structure/04: 잔여 90s 참조 0건(rg 확인).
- 행동 차이 기록: (a) NaN config → 300 폴백(워치독 죽지 않음), (b) 소규모 유닛 버짓
  조합의 web-search stall 하한 120s→330s — 의도된 기본값 상승의 귀결.
- CI: Cross-platform + service-lifecycle 전부 그린, react-doctor만 인프라 실패(#172 수정).
